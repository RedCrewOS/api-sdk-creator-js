{{#if (isObjectType) }}
{{> blockComment }}
export interface {{ title }} {
	{{#each properties as |property name|}}
		{{> comment property }}
		{{ name }}{{#unless property.required}}?{{/unless}}: {{mapType (typeRef property) }};

	{{/each}}
}
{{/if}}
