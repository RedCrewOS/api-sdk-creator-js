{{#if (isObjectType) }}
{{> description }}
export interface {{ title }} {
	{{#each properties as |property name|}}
		{{> description property }}
		{{ name }}{{#unless property.required}}?{{/unless}}: {{typeRef property }};
	{{/each}}
}
{{/if}}
